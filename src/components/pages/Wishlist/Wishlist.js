import Table from "../../layout/Table/Table";
import "./Wishlist.css";

const Wishlist = () => {
  return (
    <div className="wishlistpage">
      <h2 className="wishlistpage__title">Wishlist Page</h2>
      <Table />
    </div>
  );
};

export default Wishlist;
